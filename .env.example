# Copy this file to `.env` and fill in your values.
# `.env` is gitignored so your secrets are never committed.

# HeyGen API key — get it from https://app.heygen.com/settings (API tab)
HEYGEN_API_KEY=
